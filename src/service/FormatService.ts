import { Injectable } from '@nestjs/common';


/**
 * 云端文件管理
 */
@Injectable()
export class FormatService {
  /**
   *
   * @param deviceidstr
   */
  deviceIdFormat = (deviceidstr: string): string => {
    // s
    return 'err';
  };
}
